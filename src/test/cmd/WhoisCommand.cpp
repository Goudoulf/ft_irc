#include "../header/WhoisCommand.hpp"

void WhoisCommand::execute(int client_fd, std::map<std::string, std::string>& params, IRCServer& server)
{
    (void)client_fd;
    (void)params;
    (void)server;

}
